candidate:
  full_name: "Mohamed Abd Elrhman Azzam"
  short_name: "Mohamed Azzam"
  subtitle: "AI Engineer | AI Agent Builder | Working-Student Candidate"
  email: "mohammed.abd.elrhman.azzam@gmail.com"
  phone: "+49 152 5617 2336"
  location: "Erlangen, Germany"
  university: "Friedrich-Alexander-Universität Erlangen-Nürnberg (FAU)"
  degree_current: "M.Sc. Autonomy Technologies (Apr 2026 – Apr 2028)"
  degree_bachelor: "B.Sc. Computer and Systems Engineering (Honors)"
  linkedin: "https://www.linkedin.com/in/mohamed-azzam-61407a227/"
  github: "https://github.com/MohamedAzzam4"

target_roles:
  primary:
    - "Working Student"
    - "Werkstudent"
  scope: "all"  # technical AND non-technical

skills:
  technical:
    - "AI / Machine Learning / Deep Learning"
    - "Data Analysis (Python, Pandas, Power BI)"
    - "AI Agent Development (LangChain, n8n, RAG)"
    - "Python (OOP, FastAPI, automation)"
    - "NLP / LLM Fine-Tuning"
    - "Computer Vision"
    - "Cloud (AWS EC2, Docker)"
    - "Web Development (HTML, CSS, JS)"
    - "Web Scraping (Beautiful Soup, Selenium)"
    - "SQL / PostgreSQL"
  non_technical:
    - "Microsoft Office (Word, Excel, PowerPoint)"
    - "Data Entry and Management"
    - "Research and Documentation"
    - "Workshop Facilitation / Teaching"
    - "CRM Systems"
    - "English (C1 IELTS 7)"

preferences:
  language: "English"
  german_required: false
  hours_per_week: 20
  locations:
    - "Erlangen"
    - "Nürnberg"
    - "Bamberg"
    - "Forchheim"
    - "Herzogenaurach"
    - "Fürth"
    - "Munich"

evaluation:
  auto_cv_threshold: 3.5
  # German filter policy: reject_b1_plus (default), reject_b2_plus_only, reject_unless_bilingual, accept_all
  # reject_b1_plus          = reject B1 and B2+ (strictest — only A1-A2 or none pass)
  # reject_b2_plus_only     = reject only B2+ (B1 jobs pass through)
  # reject_unless_bilingual  = reject German jobs UNLESS the JD also requires English
  # accept_all              = don't auto-reject any German level
  german_filter: reject_b1_plus
  # OpenRouter model chain (tried in order; first success wins)
  # See https://openrouter.ai/models for available free models
  openrouter_models:
    - "openai/gpt-oss-120b:free"             # Primary — reliable, slightly slow
    - "google/gemma-4-31b-it:free"           # Backup — fast but rate-limited
    - "deepseek/deepseek-v4-flash:free"      # Fallback 1
    - "arcee-ai/trinity-large-thinking:free" # Fallback 2
    - "baidu/cobuddy:free"                   # Fallback 3
  # Google AI Studio models (for evaluation, separate from OpenRouter)
  google_models:
    - "gemma-4-26b-a4b-it"                   # Primary — more reliable
    - "gemma-4-31b-it"                       # Backup

queue_export:
  # UniversalAutoApplier-ready application queue (JSONL). Written atomically
  # by run_all.py after a successful scan + evaluate + doc-generation run.
  # Re-running overwrites the file (idempotent); a failed export fails the
  # pipeline and leaves the previous queue untouched.
  output_path: data/application_queue.jsonl
